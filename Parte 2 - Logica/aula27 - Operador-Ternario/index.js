console.clear()

// (condicao) if      (True)     else   (False)   
// (condicao) ? (valor verdadeiro) : (valor falso)

const user_points = 999;


const level_user = user_points >= 1000 ? 'VIP USER' : 'REGULAR USER';

console.log(level_user) // Output: REGULAR USER


// const user_color = null
// const default_color = user_color || 'Black'

// console.log(level_user, default_color);



// -- Operação IF/ELSE --
// if (UserPoints >= 1000){
//     console.log('VIP USER')
// } else {
//     console.log('REGULAR USER')
// };


// if (UserPoints >= 1000) { return console.log('VIP USER')}
// if (UserPoint <= 1000) { return console.log('REGULAR USER')}
