console.clear()
// Verify if word is in

const text = 'O tempo está partindo, do lugar que não mais existe.'
const verify_word = (text.includes('tempo')) ? 'Existe' : 'Não existe';

console.log(verify_word) // Output: Existe